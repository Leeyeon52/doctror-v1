name: t0703
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.0.0            # ✅ GoRouter 버전 업데이트
  provider: ^6.1.2              # ✅ MVVM ViewModel 바인딩용 Provider
  http: ^1.2.1                  # ✅ HTTP 패키지 버전 업데이트
  table_calendar: ^3.1.1        # ⭐ 캘린더 UI를 위해 추가

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1         # ✅ 안정 린트

flutter:
  uses-material-design: true    # ✅ 올바른 위치
